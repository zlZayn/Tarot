[project]
name = "ethereal-tarot"
version = "0.1.0"
description = "Ethereal Tarot 本地启动器与测试工具（Python 侧；前端构建走 npm）"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "playwright>=1.60.0",
    "rich>=13.0.0",
]

[tool.uv]
package = false